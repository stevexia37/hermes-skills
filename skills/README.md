# Skills Directory

Each subdirectory should contain one publishable Hermes skill.

Recommended layout:

```text
skills/
  skill-name/
    SKILL.md
    assets/
    examples/
```

Rules:

- `SKILL.md` must be self-contained
- avoid absolute local paths
- avoid private IDs, tokens, and user-specific routing
- document required environment variables clearly
